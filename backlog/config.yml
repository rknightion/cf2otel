project_name: "cf2otel"
default_status: "To Do"
statuses: ["To Do", "In Progress", "Parked", "Done"]
definition_of_done:
  - "just check (fmt-check, lint, vet, test, tidy-check, build, vuln)"
  - "just ci before a change that touches the Dockerfile, goreleaser or the image (adds snapshot + image)"
  - "Every new signal or attribute name declared in internal/semconv and listed in docs/signals.md"
labels: []
date_format: yyyy-mm-dd
max_column_width: 20
auto_open_browser: true
default_port: 6420
remote_operations: false
auto_commit: false
filesystem_only: false
zero_padded_ids: 4
bypass_git_hooks: false
check_active_branches: false
active_branch_days: 30
task_prefix: "cfo"
